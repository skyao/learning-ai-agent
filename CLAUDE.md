# 本仓库的工作约定

单人维护的 AI Agent 学习笔记，Hugo + Docsy 站点，正文在 `content/zh-cn/`。下面是人写内容与 AI 协助写作时共同遵守的约定。

## 文风

写作立场：以资深架构师的角度与深度展开，但不牺牲可理解性。第一人称，代入学习者的角色来收集、分析、整理。不用第三人称或上帝视角，避免高高在上的生疏感。

### 人称

- 判断、观察、结论处用「我」。
- 结构句去掉主语，或用「这一节」「这里」。
- 不出现「本笔记」「我们」「本页」「笔者」。

### 去口语

| 不用 | 改用 |
| --- | --- |
| 赌 | 依赖 |
| 把…收成 | 收缩为 / 收敛为 |
| 那一刀 / 这一刀 | 一步 |
| 塞进 | 放入 |
| 像不像 / 过不过 | 形似 / 功能正确 |
| 跑得对 | 功能正确 |
| 搞定 / 折腾 | 完成 / 处理 |
| 掉到 / 跳到 | 下降到 / 上升 |
| 埋（前因） | 预留 |
| 线头 | 线索 |
| 骨头 | 要点 |
| 卡住 | 受阻 |
| 为何 | 为什么 |
| 干脆 | 直接 |
| 其实 | 能删就删 |

### 去 AI 味

- 破折号引出的补充句改为分号或独立分句。
- `A → B → C` 箭头链改为文字表述。
- 表格内的电报体展开成完整句（「换库即换记忆」→「更换索引即更换记忆」）。
- 删「值得注意的是」「需要指出的是」「总而言之」。
- 不用 emoji 与感叹号。
- 避免三段式排比与过度对称的小标题；少用「不仅是……而是……」。

### 术语与缩写

- 缩写只用于技术专有名词（LLM、MCP、KV cache），不自造简写。
- 不把循环简写为「环」；闭环、环路、一环、环形等标准词不受影响。
- human-in-the-loop 写「人在环路」，不写「人在环」或「人在循环」。
- `vs` 改为「与」；`+` 改为「加」或「与」。

### 严谨与中立

- 结论标注依据：论文第几节、哪次实验、哪个来源。
- 无法核实就写「待核对」，不靠语气词加强。
- 厂商宣称与第三方谓词分开陈述；不用营销语汇，也不反向嘲讽。

### 改写纪律

- 只动表达。事实、数字、结论、表格结构、跨页链接、frontmatter 一律不动。
- 优先定点修改，不整页重写——整页重写容易在无意识处改动事实。
- 内容项与文风项分成不同提交，便于逐项核对。

## 内容边界

- 沙箱只写能力与约束，不写实现细节：约束句留，手段句删。
- 时间线主线只收以 LLM 为决策核心的软件系统。符号规划器、BDI、纯 RL、机器人、自动驾驶只作前传或相邻领域点名，不进主叙事。
- 分类与标签的定义见 `content/zh-cn/timeline/overview/research/`，写作时以那里为准。

## 采信

- 每条至少一个一手来源：论文、官方公告、可复现仓库。
- 只有二手来源时明确标注「据媒体报道」；SEO 垃圾站的孤证一律不写。
- 不把后来才清楚的重要性写成当时已注定。

## 提交

单人仓库，直接提交 main，不建分支。内容项与文风项分开提交。

## 命令

- 构建：`npm run build`
- 改完内容必须跑 `npm run check:links`，退出码 0 才算通过。它对着一份压缩过的构建产物校验，因此改动正文后要重新构建再校验。
