---
title: "SWE-agent 讲解"
linkTitle: "讲解"
weight: 20
date: 2026-09-15
description: >
  给 Agent 的界面和给人的 IDE 不是同一张桌子。开源对照闭源「软件工程师」。
---

## 论文解决了什么问题

Devin 把「自主修 issue」做成舆论。学界缺一个可复现的对照：同样用 GPT-4 级模型，循环怎么接计算机，分数才上得去。SWE-agent 的答案不是再写一套更炫的多 Agent，而是 **改界面**：搜索、编辑、测试的反馈要短、要可解析、要挡住常见自杀式操作。这是 2022 年 ReAct「行动是自由文本」之后，编码线上把 Action 重新设计成「给模型用的 API」。

按 AI Agent 定义，SWE-agent **是** Agent：决策、在仓库环境里行动、用测试回执再决策。

## 对后续 LLM 与 Agent 的影响

后来的编码运行时（OpenHands、Claude Code、Codex CLI）都在做同一件事的产品版：不是把 VS Code 像素原样交给模型，而是给一套模型吃得消的工具。ACI 这个词没有全部流行起来，这个设计问题留下来了。

12.5% 和 Devin 的 13.86% 外观接近，口径不同。真正要记的是：2024 年开源循环第一次在 SWE-bench 上拿出可引用的双位数，而且论文把「接口」写成自变量。刷分后来靠更强模型和 Verified 子集；界面这一课没有作废。
